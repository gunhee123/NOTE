TYPE SCRIPT

[변수 타입]
Number, String, Object, Array, void, null, undefined, boolean, 
Tuple, Enum, any, never

Typle : 배열의 길이가 고정되고 각 요소의 타입이 지정되어 있는 배열 형식 (인덱스위치 변경가능)
	//let arr: [sring, number] = ['hi', 10];
Enum : 상수의 집합
	//enum Avengers { apt, IronMan, Thor }
	//let hero : Avengers = Avengers.Capt;	// = Avengers[0];
any : 모든 타입 허용
never : 끝나지 않는 함수를 의미
	//function neverEnd(): never {
	//  while(true) {
	//  }
	//}

***

[함수 타입 선언]
function sum(a: number, b: number): number {	//b?: number 사용시 기존 js와 동일
  return a + b;
}

***

[인터페이스]
객체의 속성과 속성의 타입 정의
class myBeer implements CraftBeer { }		//interface CraftBeer { }
CraftBeerd의 속성을 따름

[클래스]
class FrontEndDeveloper extends Developer { }	//abstract class Developer { }

[타입스크립트 핸드북](https://joshua1988.github.io/ts/guide/classes.html#accessor, "설명")